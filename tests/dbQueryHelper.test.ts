import { describe, it } from "@jest/globals";
import {dbConnectionAndQuery} from "../src/app/dbQueryHelper"

describe("DB query helper connection", () => {
    it("Can connect to correct db without throwing an exception", () => {
    })
});
