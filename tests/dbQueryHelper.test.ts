import { describe, it } from "@jest/globals";
import { executeQueryAsync } from "@/app/dbQueryHelper";

describe("DB query helper connection", () => {
    it("Can connect to db without throwing an exception when user details are correct", () => {
    })
});
